# Hoisted node_modules (npm-flat layout) so migrating this existing app to a pnpm
# workspace doesn't surface phantom-dependency resolution errors. Can be tightened
# to pnpm's default isolated linker later once every package declares its deps.
node-linker=hoisted
